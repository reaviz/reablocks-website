import { Canvas, Meta } from '@storybook/blocks';
import * as NotFoundStories from '../samples/NotFound.story';

<Meta of={NotFoundStories} />

# Not Found

The Not Found block provides a way to provide the user with feedback that something is wrong
or when a user tries to access a page they do not have access to.

## We Lost This Page
<Canvas of={NotFoundStories.NotFound} />

## Does Not Exist
<Canvas of={NotFoundStories.DoesNotExist} />

## Lost Page
<Canvas of={NotFoundStories.LostPage} />
