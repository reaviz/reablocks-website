import { Canvas, Meta } from '@storybook/blocks';
import * as RegisterStories from '../samples/Register.story';

<Meta of={RegisterStories} />

# Register

The Register block facilitates new user registrations, capturing essential information to create user accounts.

<Canvas of={RegisterStories.Register} />

## RegisterFull
<Canvas of={RegisterStories.RegisterFull} />

## RegisterV2
<Canvas of={RegisterStories.RegisterV2} />

## RegisterWithSocial
<Canvas of={RegisterStories.RegisterWithSocial} />
