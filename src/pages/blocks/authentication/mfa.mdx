import { Canvas, Meta } from '@storybook/blocks';
import * as MfaStories from '../../../stories/blocks/Mfa.story';

<Meta of={MfaStories} />

# MFA

The MFA block facilitates capturing a user's MFA code and verifying it.

<Canvas of={MfaStories.Mfa} />

## MFA Login
<Canvas of={MfaStories.MfaLogin} />

## MFA Authenticator
<Canvas of={MfaStories.MfaAuthenticator} />
