import { PropsTable } from '@/components/ui/props-table';
import { StoryRenderer } from '@/components/ui/story-renderer';

# Collapse
Simple expand / collapse component with animations via framer-motion.

## Examples
<StoryRenderer path="components/Collapse.story.tsx" name="Simple" storybookKey="components-layout-collapse--simple" />

### Custom Animation
Define custom animations for the collapse, using the `animation`, `animationViewChange` props:
<StoryRenderer path="components/Collapse.story.tsx" name="CustomAnimation" storybookKey="components-layout-collapse--custom-animation" />

Learn more about how to customize the animations in the [Custom Animations](/docs/theme/custom-animations) section.

## API
<PropsTable name="Collapse" />
