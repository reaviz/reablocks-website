import { PropsTable } from '@/components/ui/props-table';
import ThemeRender from '@/components/ui/theme-render';
import { StoryRenderer } from '@/components/ui/story-renderer';

# Tooltip
A Tooltip is a small, informative pop-up that provides context, explanations, or additional details when a user hovers over or interacts with specific elements in a user interface.

## Examples
### Basic
<StoryRenderer path="components/Tooltip.story.tsx" name="Simple" storybookKey="components-layers-tooltip--simple" />
### Disabled
<StoryRenderer path="components/Tooltip.story.tsx" name="Disabled" storybookKey="components-layers-tooltip--disabled" />
### Custom Animation
Define custom animations for the tooltip, using the `animation`, `animationViewChange` props:
<StoryRenderer path="components/Tooltip.story.tsx" name="CustomAnimation" storybookKey="components-layers-tooltip--custom-animation" />

Learn more about how to customize the animations in the [Custom Animations](/docs/theme/custom-animations) section.

## Theme
This component uses the following default theme:

<ThemeRender name="tooltip" />

Learn more about how to customize in the [Theme documentation](/docs/theme/getting-started).

## API
<PropsTable name="Tooltip" />
