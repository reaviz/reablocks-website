import Mdx from '../../components/ui/mdx';

export async function Changelog() {
  const response = await fetch('https://raw.githubusercontent.com/reaviz/reablocks/master/CHANGELOG.md');
  const text = await response.text();
  return (
    <Mdx>
      {text}
    </Mdx>
  );
}

<Changelog />