import { StoryRenderer } from '@/components/ui/story-renderer';

# Not Found

The Not Found block provides a way to provide the user with feedback that something is wrong
or when a user tries to access a page they do not have access to.

## We Lost This Page
<StoryRenderer path="blocks/NotFound.story.tsx" name="NotFound"/>

## Does Not Exist
<StoryRenderer path="blocks/NotFound.story.tsx" name="DoesNotExist"/>

## Lost Page
<StoryRenderer path="blocks/NotFound.story.tsx" name="LostPage"/>
