import { StoryRenderer } from '@/components/ui/story-renderer';

# Chat

The chat block provides a way to provide the user the ability to send messages.

## Report an Issue

<StoryRenderer path="blocks/Support.story.tsx" name="Support"/>
